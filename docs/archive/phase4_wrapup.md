Let's wrap up the backend architectural phase. We have a rock-solid plan.

Since I am running this in the CLI, I need the actual code. Please output the complete, production-ready, copy-pasteable code blocks for all the files we have finalized over these three phases:
1. `schema.sql` (Phase 1)
2. `main.py` (Phase 2 - FastAPI skeleton with the ~~[CANCELLED: sliding window]~~, deduplication cache, and pessimistic locking)
3. `Dockerfile`, `requirements.txt`, `render.yaml`, and `.env.example` (Phase 3)

Please format each file in its own clear markdown code block with the filename at the top so I can easily copy them into my IDE.
