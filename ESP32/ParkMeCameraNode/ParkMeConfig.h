// Arduino copies the sketch folder to a temporary build directory before
// compiling, so a relative include like "../SECRETS.h" does not work here.
// Each teammate must point this at the SECRETS.h in their own clone:
#include "C:/Users/georg/source/repos/IOT_ParkMe/ESP32/SECRETS.h"
